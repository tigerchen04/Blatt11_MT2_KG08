#include <iostream>
#include <thread>
#include <chrono>

using namespace std;
using namespace chrono;

void task() {
	int lokale_Zählvariable = 0;
	do
	{
		cout << "Ich bin Thread " << this_thread::get_id() << " und mein Zählstand ist " << lokale_Zählvariable << "." << endl;
		++lokale_Zählvariable;
		this_thread::sleep_for(milliseconds(100));
	} while (lokale_Zählvariable >= 0);
};


int main() {
	thread t1(task);
	thread t2(task);
	thread t3(task);
	thread t4(task);
	thread t5(task);
	thread t6(task);
	thread t7(task);
	thread t8(task);
	thread t9(task);
	thread t10(task);
	thread t11(task);
	thread t12(task);

	t1.join();
	t2.join();
	t3.join();
	t4.join();
	t5.join();
	t6.join();
	t7.join();
	t8.join();
	t9.join();
	t10.join();
	t11.join(); 
	t12.join();

}
